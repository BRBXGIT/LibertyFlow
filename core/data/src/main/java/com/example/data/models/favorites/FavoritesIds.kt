package com.example.data.models.favorites

/**
 * A specialized [ArrayList] of unique identifiers (IDs).
 *
 * Typically used to represent a user's list of favorite anime IDs
 * for quick lookup or synchronization with local storage.
 */
class FavoritesIds: ArrayList<Int>()