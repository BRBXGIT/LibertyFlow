package com.example.data.models.favorites

/**
 * A simple data wrapper for an anime ID within the context of
 * a favorites operation.
 *
 * @property id The unique identifier of the anime marked as favorite.
 */
data class FavoriteItem(val id: Int)