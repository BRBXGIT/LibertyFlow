package com.example.data.models.releases.anime_id

/**
 * A type-safe wrapper representing the unique identifier of an anime
 * * Used only in random anime.
 * @property id The raw integer value of the anime identifier.
 */
data class AnimeId(val id: Int)